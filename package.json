{
  "name": "andorra-private",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-sitemap.mjs && astro build && node scripts/assert-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "vercel": "^59.5.0"
  }
}
